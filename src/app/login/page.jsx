import AuthScreen from "../components/auth/AuthScreen";

export const metadata = {
    title: "Sign in — CraftSite",
    description: "Log in or create your CraftSite account.",
};

export default function LoginPage() {
    return <AuthScreen />;
}